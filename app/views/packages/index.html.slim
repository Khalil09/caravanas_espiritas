h1 Listing packages

table
  thead
    tr
      th Name
      th Description
      th
      th
      th

  tbody
    - @packages.each do |package|
      tr
        td = package.name
        td = package.description
        td = link_to 'Show', package
        td = link_to 'Edit', edit_package_path(package)
        td = link_to 'Destroy', package, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New Package', new_package_path
