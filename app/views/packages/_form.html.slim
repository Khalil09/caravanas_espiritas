= form_for @package, :html => { :multiparts => true } do |f|
  - if @package.errors.any?
    #error_explanation
      h2 = "#{pluralize(@package.errors.count, "error")} prohibited this package from being saved:"
      ul
        - @package.errors.full_messages.each do |message|
          li = message

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description

    = f.nested_fields_for :years do |ff|
      = ff.label "Anos a serem colocados"
      = ff.text_field :name
      = ff.remove_nested_fields_link
  = f.add_nested_fields_link :years

  = f.file_field :image
  .actions = f.submit
